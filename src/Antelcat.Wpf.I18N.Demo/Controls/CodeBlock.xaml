<UserControl x:Class="Antelcat.Wpf.I18N.Demo.Controls.CodeBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Antelcat.Wpf.I18N.Demo.ViewModels"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance vm:CodeFragmentViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <ListView
        ItemsSource="{Binding Codes}">
        <!--<ListView.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
        </ListView.ItemsPanel>-->
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding Text}" Foreground="{Binding Color}"></TextBlock>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>
